$(document).ready(function() {
     var listo = [];
     var Task = function(task) {
     	this.task = task;
     	this.id = new;
     }
     var addTask = function() {
        if(task) {
        	task = new Task(task);
        	listo.push(task);

        }

     }









});